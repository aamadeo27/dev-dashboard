# toast:show on run terminal events

**Tag**: backend
deps: T4.3, T6.1

- **Description**: On `run:finished`, emit `toast:show` with kind=completed/failed/stopped, the sequence + project names, and the `run_id`.
- **Acceptance**:
  - Each terminal state emits exactly one toast.
