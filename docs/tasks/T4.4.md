# T4.4 — Attached-md Context Handling

**Epic**: 4 — Run Execution Core  
**Type**: Backend (Rust)  
**Branch**: `feat/T4.4-attached-md`  
**Worktree**: `../dev-dashboard-T4.4`

## Description

When `attached_md_path` is set on `LaunchInput`, prepend the file's content to the first stdin write (or pass as a CLI arg if Claude CLI supports it — pick based on T1.2 probe). Record the path in `meta.json`.

## Acceptance Criteria

- File contents reach the child process on the first stdin write when `attached_md_path` is set.
- Missing file at launch time → `AppError::NotFound` before spawn.
- `meta.json` records `attached_md_path`.

## Key Files

- `docs/tasks/T1.2.md` — CLI probe findings (arg support check)
- `src-tauri/src/runs/session.rs` — RunSession, first stdin write
- `src-tauri/src/runs/manager.rs` — RunManager, LaunchInput handling
- `src-tauri/src/ipc/runs.rs` — IPC launch handler
- `docs/kb/contracts/run.md` — Run and meta.json contract
- `docs/kb/contracts/ipc-runs.md` — IPC contract for launch_run

## Serialization Note

This task modifies RunSession's first-stdin-write path. T4.7 also touches RunSession (step-failure re-invoke). **T4.4 must merge before T4.7 starts.** Keep changes scoped to the first-write hook only.

## Dependencies

- T4.3 done (RunSession available)
