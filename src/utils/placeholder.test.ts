// Placeholder test file — ensures vitest finds at least one test suite.
// Remove this file once real tests are added.
describe("scaffold", () => {
  it("passes", () => {
    expect(true).toBe(true);
  });
});
