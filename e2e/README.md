# E2E Tests

Tauri WebDriver e2e tests. Requires:
1. Rust installed (`rustup`)
2. App compiled (`pnpm build`)
3. `pnpm add -D @tauri-apps/driver webdriverio`

See KB §2.7 for test stack. Implement as part of Epic 9 (T9.x).
